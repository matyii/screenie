const auth = require('./auth');
const checkFolder = require('./check');
const loadRoutes = require('./loadRoutes');
const config = require('./config');

module.exports = { auth, checkFolder, loadRoutes, config };